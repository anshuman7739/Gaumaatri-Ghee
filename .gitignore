# Environment variables — NEVER commit credentials
.env
.env.local
.env.*.local

# Node.js
node_modules/
npm-debug.log*

# Runtime order storage
orders.json
# Influencer/coupon engine DB (source of truth) — never commit
influencers-data.json

# OS
.DS_Store
Thumbs.db
